import { THEME } from "../types";

const THEME: THEME = {
  penColor: {
    light: "black",
    dark: "white",
  },
  backGroundColor: {
    light: "white",
    dark: "black",
  },
};

export { THEME };
